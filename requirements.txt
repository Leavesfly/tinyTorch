# tinyTorch has NO runtime dependencies (pure Python implementation).
# Development dependencies are defined in pyproject.toml [project.optional-dependencies.dev].
# Install them with:  pip install -e ".[dev]"
